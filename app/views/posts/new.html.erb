<div class="container">
	<h1>New Post</h1>

	<%= form_for @post, :url => posts_path do |f| %>
		<div class="form-group">
			<%= f.label :title %>
			<%= f.text_field :title %>

			<%= f.label :content %>
			<%= f.text_field :content %>
			
			<%= f.submit %>
		</div>
	<% end %>

</div>